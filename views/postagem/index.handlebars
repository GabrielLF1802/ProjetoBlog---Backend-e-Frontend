
<div class="card">
    <div class="card-body">
        <h2>{{postagens.titulo}}</h2>
        <small>Data da publicação: {{postagens.data}}</small>
        <hr>
        <p>{{postagens.conteudo}}</p>
    </div>
</div>

