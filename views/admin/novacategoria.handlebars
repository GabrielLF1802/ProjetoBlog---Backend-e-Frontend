<div class="card">
    <div class="card-body">
        <form action="/admin/nova" method="POST">
            <label for="nome" class="form-label">Nome:</label>
                <input type="text" id="nome" placeholder="Nome da categoria" class="form-control">
    
    
            <label for="slug" class="form-label">Slug:</label>
                <input type="text" id="slug" placeholder="Slug da categoria" class="form-control">
        </form>
        <br>
        <button class="btn btn-success">Criar categoria</button>
    </div>
</div>